{
  "name": "demo",
  "version": "1.0.0",
  "description": "demo teste",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "executar": "rm -f cypress/reports/mochawesome_* && npx cypress run && npx mochawesome-merge cypress/reports/*.json -o cypress/reports/mochawesome.json && node sendMetrics.js"
  },
  "author": "rafael suzano cruz",
  "license": "ISC",
  "devDependencies": {
    "cypress": "^13.6.6",
    "cypress-mochawesome-reporter": "^3.8.2",
    "mochawesome-merge": "^4.4.1"
  },
  "dependencies": {
    "@influxdata/influxdb-client": "^1.35.0",
    "cypress-plugin-api": "^2.11.1"
  }
}
